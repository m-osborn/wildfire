#!/usr/bin/env node
var debug = require('debug')('wildfire');
var app   = require('../server');

app.set('port', process.env.PORT || 3000);

var server = app.listen(app.get('port'), function() {
  console.log('Wildfire is now watching on port ' + server.address().port);
});
